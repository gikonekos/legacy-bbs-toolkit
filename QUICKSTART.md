# クイックスタートガイド

このガイドは、プログラミングに詳しくない方でも `legacy-bbs-toolkit` を使えるように、
一番シンプルな使い方だけをまとめたものです。詳しい仕様や設計方針を知りたい方は
[README.md](README.md) を参照してください。

## 1. このツールは何をするもの？

昔ながらの日本語BBS（掲示板）が出力したHTML形式の過去ログを読み込み、
投稿1件ごとに区切られたデータ（JSON Lines形式）に変換します。
どのBBSシステムが出力したログかは、ほとんどの場合ツールが自動で判定します。

## 2. インストール

Python 3.9以降がインストールされていれば、以下のコマンド1行で使えるようになります。

```bash
pip install legacy-bbs-toolkit
```

「`pip`ってなに？」という場合は、まずお使いのPCにPythonがインストールされているか
確認してください（[python.org](https://www.python.org/) からダウンロードできます）。
インストール時に「Add Python to PATH」にチェックを入れておくと、後の作業がスムーズです。

## 3. 使ってみる

変換したいログファイル（例：`200907.html`）を用意し、以下のように実行します。

```bash
legacy-bbs-toolkit 200907.html
```

これだけです。うまくいけば、次のような結果が表示されます。

```
========================================
 legacy-bbs-toolkit 解析結果
========================================

✅ 正常に処理できました。

  使用したパーサー : kscrr1p9系（あやしいわーるど＠暫定・zbbs.cgi系列）
  判定方法         : 自動判定
  投稿件数         : 410件
  警告             : なし
  出力先           : 200907.ir.jsonl
```

`200907.ir.jsonl` というファイルが同じ場所に作られています。これが変換結果です。

## 4. 結果の見方

- **✅ 正常に処理できました** … 特に問題なく変換できています。そのまま使って大丈夫です。
- **⚠️ 警告があります** … 変換自体はできていますが、想定外の箇所が見つかっています。
  下に表示される警告メッセージの内容を確認してください（元のログファイル自体が
  壊れているわけではないことがほとんどです）。
- **投稿件数** … 実際に読み取れた投稿の数です。元のログを開いて投稿数がだいたい
  合っているか、目安として確認するとより安心です。
- **判定方法** … 「自動判定」であれば、ツールが自力でBBSの種類を見分けています。

## 5. うまくいかないとき

### 「error: no parser can handle this input」と表示される

対応しているBBSシステムのどれにも一致しなかった、という意味です。
現時点で対応しているのは以下の系統です（詳細は[README.md](README.md#対応系統一覧)）。

- あやしいわーるど＠暫定 / zbbs.cgi 系列（`kscrr1p9`）
- kuzuha-scriptPHP+ 系列（`ksphp_plus`）
- リミックス系統（`remix`）
- みずいろ／ダーザイン系統（`mizuiro`）
- メイソ系統（`meiso`）
- 有明／MINIBBS系統（`ariake`）

これら以外のBBSシステムのログは、現時点では未対応です。

### 文字化けする

`--from` オプションで文字コードを明示的に指定できます。

```bash
legacy-bbs-toolkit 200907.html --from cp932
```

`cp932`（Shift_JIS系）、`euc_jp`、`utf-8` のいずれかを指定できます。
通常は指定しなくても自動判定されますが、判定に失敗した場合はお試しください。

### もっと詳しい情報が見たい

`--verbose` オプションを付けると、パーサーの内部識別子（namespace）など、
技術的な詳細情報も追加で表示されます。

```bash
legacy-bbs-toolkit 200907.html --verbose
```

## 6. 次のステップ

- ファイル出力せずに結果だけ確認したい場合は `--summary-only` オプション
- 自動判定を使わず、特定のパーサーを指定したい場合は `--override` オプション

これらの詳しいオプションについては [README.md](README.md) を参照してください。
